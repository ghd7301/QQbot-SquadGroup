# Squad 地图知识能力待办

更新时间：2026-07-23

## 目标

让 QQBot 能结合 Squad 地图回答空间相关问题，例如：

- 某张地图整体是什么地形。
- 哪些位置有河流、湖泊或沼泽。
- 哪些位置是城区、村庄、工业区或开阔地。
- 桥梁、道路和固定过河点在哪里。
- 某旗点附近有哪些地形和关键设施。
- 从一个位置到另一个位置是否需要过河。
- 某条路线是否适合步兵、后勤或装甲通行。
- 必要时向群里发送带标记的局部地图。

本计划不把地图图片直接塞进现有 Markdown 知识库。现有 `knowledge/` 继续保存规则、术语和战术经验；地图图片和空间数据单独管理。

## 核心结论

1. 仅在 Markdown 中写 `![地图](map.png)`，当前 Bot 不会真正读取图片像素，只会把它当成文字路径。
2. 不要把整张图片编码成 Base64 存进 Markdown。
3. 第一阶段不需要完整游戏原始数据，高清地图加人工结构化标注即可。
4. 要做高程、视野、坡度、涉水和精确通行分析时，再使用 Squad SDK 可合法导出的高度图和场景数据。
5. 地图问答应以结构化空间数据为事实来源，视觉模型只用于辅助识图和解释。

## 建议目录结构

未来新增独立目录，不放入现有 `knowledge/`：

```text
map_knowledge/
  narva/
    overview.png
    description.md
    manifest.json
    features.geojson
    aliases.json
    layers.json
    heightmap.png
    tiles/
  mutaha/
    overview.png
    description.md
    manifest.json
    features.geojson
    aliases.json
    layers.json
```

文件用途：

- `overview.png`：高清战术地图原图。
- `description.md`：地图整体特点、社区经验和文字说明。
- `manifest.json`：地图尺寸、图片尺寸、坐标原点、比例、方向、数据版本和来源。
- `features.geojson`：水域、城区、桥梁、道路、树林、高地等空间要素。
- `aliases.json`：中文名、英文名、社区俗称和拼写变体。
- `layers.json`：不同模式/Layer 的旗点、阵营和主基地信息。
- `heightmap.png`：可选的 16 位高度图，用于后期高程和坡度分析。
- `tiles/`：可选的地图切片，用于局部查询和视觉模型输入。

## 结构化要素类型

`features.geojson` 需要逐步覆盖：

- `water`：河流、湖泊、沼泽、水渠。
- `bridge`：桥梁、固定过河点、涉水点。
- `urban`：城区、村庄、工业区、建筑群。
- `road`：主路、土路、铁路。
- `forest`：树林和密集植被区。
- `open_field`：开阔地和农田。
- `hill`：高地、山脊、谷地。
- `poi`：标志性建筑、路口、社区常用地名。
- `flag`：旗点和可能的刷点区域。
- `main_base`：主基地。

示例：

```json
{
  "type": "Feature",
  "properties": {
    "category": "bridge",
    "name": "北桥",
    "aliases": ["上桥", "北边桥"],
    "source": "人工标注",
    "verified": true
  },
  "geometry": {
    "type": "LineString",
    "coordinates": [[0.32, 0.41], [0.35, 0.44]]
  }
}
```

第一阶段可以使用 0 到 1 的标准化图片坐标，后续再校准为游戏世界坐标。

## 坐标系统待办

- [ ] 确定地图图片左上角和游戏世界原点的对应关系。
- [ ] 记录地图图片宽度和高度。
- [ ] 记录游戏地图实际宽度和高度（米）。
- [ ] 确认地图是否存在旋转、裁边或非正方形区域。
- [ ] 建立世界坐标、标准化坐标和像素坐标的双向转换。
- [ ] 每张地图选择至少 3 个明显地标作为校准点。
- [ ] 验证两点距离和游戏内测距是否基本一致。

坐标关系：

```text
游戏世界坐标（厘米/米）
  <-> 标准化地图坐标（0 到 1）
  <-> 图片像素坐标
```

## 数据来源待办

优先顺序：

1. Squad 官方 SDK 可合法导出的数据。
2. 官方或社区公开地图资料。
3. 高清地图图片加人工标注。
4. 用户和战队成员提供的社区地名、路线和战术经验。

- [ ] 为每张地图记录来源网址或来源说明。
- [ ] 记录获取日期、游戏版本和地图版本。
- [ ] 确认图片和导出数据是否允许在项目中保存和分发。
- [ ] 不公开分发未经许可提取的游戏资源。
- [ ] 不把版本相关内容描述成永久不变的官方事实。

## 第一阶段：文字地图知识 MVP

目标：不修改地图查询代码，先准备可供人工检查的数据。

- [ ] 选择 2 到 3 张常用地图试点。
- [ ] 建议优先 Narva、Mutaha，再选择一张地形更复杂的地图。
- [ ] 收集每张地图的高清战术地图 PNG。
- [ ] 为每张地图编写 `description.md`。
- [ ] 整理地图中文名、英文名和社区叫法。
- [ ] 描述整体地形、水域、城区、主要道路和桥梁。
- [ ] 标明哪些描述是地图事实，哪些是社区战术经验。
- [ ] 为每张地图准备 10 到 20 个真实测试问题。

这一阶段能够回答概括问题，但不能保证精确距离和空间关系。

## 第二阶段：结构化空间查询

目标：让程序先计算空间事实，再让模型组织回答。

- [ ] 建立 `map_knowledge/` 数据加载器。
- [ ] 读取 `manifest.json`、`features.geojson`、`aliases.json` 和 `layers.json`。
- [ ] 识别用户问题中的地图名、旗点名和社区别名。
- [ ] 支持点是否位于水域/城区/树林等区域内。
- [ ] 支持查询指定位置附近的桥梁、道路和 POI。
- [ ] 支持计算两点距离和方位。
- [ ] 支持判断两点连线是否穿过水域。
- [ ] 支持查询不同 Layer 的旗点和主基地。
- [ ] 把计算结果转换成简洁文字事实交给 LLM。
- [ ] 空间数据不足时明确说明不确定，不让模型猜测。

建议回答上下文格式：

```text
地图：Narva
目标位置：火车站
北侧 420 米存在河流
东北方向 610 米存在桥梁“北桥”
目标与桥梁之间有主路连接
数据版本：2026-07
```

## 第三阶段：视觉模型辅助

目标：必要时让视觉模型查看相关局部地图，而不是每次发送整张大图。

- [ ] 确认当前 LLM 服务和模型是否支持图像输入。
- [ ] 修改模型调用接口，使其支持图文 `content`。
- [ ] 查询前根据目标坐标裁剪地图局部区域。
- [ ] 对裁剪图添加地图名、方向、比例尺和位置标记。
- [ ] 本地图片通过 Base64 data URL 或模型可访问的临时 URL 传入。
- [ ] 限制裁剪尺寸，避免延迟和费用失控。
- [ ] 视觉结果只作辅助，不覆盖结构化空间事实。
- [ ] 记录视觉模型使用的图片范围和回答结果，便于审计。

当前 `squad_bot/llm.py` 使用纯文本消息。即使模型服务支持视觉，也需要后续单独改造调用格式。

## 第四阶段：向 QQ 群发送地图

- [ ] 根据用户问题生成局部地图裁剪图。
- [ ] 在图上标记目标位置。
- [ ] 高亮桥梁、水域、城区或推荐路线。
- [ ] 添加方向箭头和比例尺。
- [ ] 通过 NapCat/OneBot 发送本地图片或 Base64 图片。
- [ ] 图片发送失败时回退到纯文字回答。
- [ ] 限制群聊发送频率，避免刷图。

## 第五阶段：高程、路线与通行分析

这一阶段需要更完整的地图原始数据或 SDK 导出数据。

- [ ] 获取合法的高度图。
- [ ] 建立高程、坡度和山脊数据。
- [ ] 标注道路等级、桥梁宽度和涉水点。
- [ ] 建立步兵、轮式车辆、履带车辆的不同通行成本。
- [ ] 支持计算路线是否跨水、是否经过城区和开阔地。
- [ ] 支持基础路线推荐，而不是只计算几何最短距离。
- [ ] 后期评估视线和遮挡分析，但不要在数据不足时宣称精确视野。

## 回答流程设计

```text
用户问题
  -> 识别地图、Layer、位置和问题类型
  -> 检索普通文字知识
  -> 查询结构化地图数据
  -> 必要时裁剪局部地图并交给视觉模型
  -> 汇总事实、距离、方向和不确定性
  -> LLM 生成自然中文回答
  -> 可选：向 QQ 群发送带标记的地图图片
```

模型负责组织语言，不负责凭空生成地图事实。

## 测试问题清单

基础地图描述：

- [ ] `Narva 这张图大概长什么样？`
- [ ] `Mutaha 城区多不多？`
- [ ] `这张图哪里比较开阔？`

空间关系：

- [ ] `火车站北边有没有河？`
- [ ] `离火车站最近的桥在哪里？`
- [ ] `从 A 点到 B 点需要过河吗？`
- [ ] `这个旗点附近是城区还是野地？`

别名和纠错：

- [ ] 中文名、英文名和社区俗称指向同一地点。
- [ ] 地名拼错一两个字时仍能识别。
- [ ] 同名地点存在于不同地图时要求用户确认地图。
- [ ] 用户没有提供 Layer，但问题依赖旗点时要求补充 Layer。

不确定性：

- [ ] 数据未标注时明确说暂时没有可靠数据。
- [ ] 不根据整张缩略图猜测小桥和涉水点。
- [ ] 不把社区经验描述为官方机制。
- [ ] 地图版本变化时提醒数据版本可能过期。

## 验收标准

第一阶段：

- 至少 2 张地图有高清图片、文字说明、别名和测试问题。
- Bot 能通过普通文字知识回答地图整体特点。

第二阶段：

- 至少 2 张地图完成水域、城区、桥梁、道路和主要 POI 标注。
- 能可靠回答包含、距离、方向和邻近查询。
- 结构化查询有单元测试，不依赖 LLM 判断正确与否。

第三/四阶段：

- 能按位置裁剪并标记地图。
- 能向模型发送局部图片，也能向 QQ 群发送结果图片。
- 图片能力失败时不影响文字知识问答。

第五阶段：

- 有可靠的高程和通行数据来源。
- 不同载具类型使用不同路线成本。
- 路线回答能说明依据和数据版本。

## 实施原则

- 不改动现有 `knowledge/` 内容，除非用户之后明确要求。
- 地图原始资源、结构化空间数据和普通 Markdown 知识分开存储。
- 优先做少量地图的高质量试点，不一次导入全部地图。
- 每条结构化数据记录来源、版本和校验状态。
- 事实查询优先使用空间数据，视觉模型只作辅助。
- 用户提供的社区叫法和战术经验应保留，但与官方/地图事实分层。
- 任何代码改造前先和用户确认试点地图、数据来源和回答精度。
